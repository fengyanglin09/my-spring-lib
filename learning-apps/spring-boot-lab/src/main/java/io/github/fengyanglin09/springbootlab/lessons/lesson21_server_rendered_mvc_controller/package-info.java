/**
 * Optional future lesson: server-rendered MVC with regular {@code @Controller}.
 *
 * <p>This package intentionally starts with only notes. The main Spring Boot lab
 * path focuses on backend REST APIs with {@code @RestController}. Regular
 * {@code @Controller} is important too, but its common use case leads into
 * server-rendered web pages: view names, templates, {@code Model}, browser
 * forms, and redirects.</p>
 *
 * <p>Planned study path:</p>
 *
 * <ol>
 *     <li>Create a regular {@code @Controller}.</li>
 *     <li>Return view names instead of JSON response bodies.</li>
 *     <li>Use a template engine such as Thymeleaf.</li>
 *     <li>Pass data to a page with {@code Model}.</li>
 *     <li>Handle form submissions and redirects.</li>
 *     <li>Compare {@code @Controller} plus {@code @ResponseBody} with
 *     {@code @RestController}.</li>
 * </ol>
 */
package io.github.fengyanglin09.springbootlab.lessons.lesson21_server_rendered_mvc_controller;
