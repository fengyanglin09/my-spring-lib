# Lesson 13: Calling Other Services

## Status

Planned lesson. This README keeps the lesson path linked while the hands-on code
is still waiting to be built.

## Objective

Call downstream HTTP services from Boot code without hiding timeouts, failures,
or response mapping.

## Planned Study

- Use `RestClient`.
- Wrap outbound calls in typed client classes.
- Configure timeouts.
- Map downstream failures clearly, with Vavr `Try` or `Either` only where useful.
